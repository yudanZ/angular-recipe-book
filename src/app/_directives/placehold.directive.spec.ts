import { PlaceholdDirective } from './placehold.directive';

describe('PlaceholdDirective', () => {
  it('should create an instance', () => {
    const directive = new PlaceholdDirective();
    expect(directive).toBeTruthy();
  });
});
